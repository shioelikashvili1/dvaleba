function greet(name) {
    console.log(`გამარჯობა ${name}, წარმატებულ დღეს გისურვებ!`);
  }
  
  // გამოსაყენებლად:
  greet('გიორგი');  // გამარჯობა გიორგი, წარმატებულ დღეს გისურვებ!
  
  function rectangleArea(length, width) {
    return length * width;
  }
  
  // გამოსაყენებლად:
  console.log(rectangleArea(5, 3));  // 15
  